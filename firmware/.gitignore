# WiFi credentials - keep secure!
wifi.env

# MQTT broker credentials - keep secure! (copy mqtt_config.example.h)
src/mqtt_config.h

# Google Calendar OAuth credentials - keep secure! (copy calendar_config.example.h)
src/calendar_config.h

# Build artifacts
.pio/
compile_commands.json
.vscode/

# OS files
.DS_Store
Thumbs.db
